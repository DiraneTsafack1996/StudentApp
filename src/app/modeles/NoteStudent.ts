export class Note {

    
    

    FachId ? : string
    FachName ? : string


    StudiengangId ? : string // deja
    StudiengangName ? : string // deja

    StudentFirstName ? :  string 
    StudentEmailuid ? : string

    CreditPunkte ? : string



    Note : string

    Note_ref ? : string
}